{
  "name": "third-orbit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch-font": "node scripts/fetch-font.mjs",
    "precompute": "python precompute/precompute.py",
    "setup": "npm run fetch-font && npm run precompute",
    "dev": "vite",
    "build": "npm run setup && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.0",
    "typescript": "^5.6.0"
  }
}
