{
  "name": "third-orbit",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch-font": "node scripts/fetch-font.mjs",
    "precompute": "tsx scripts/precompute.ts",
    "setup": "npm run fetch-font && npm run precompute",
    "dev": "vite",
    "build": "npm run setup && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^8.0.0"
  },
  "packageManager": "npm@11.14.0"
}